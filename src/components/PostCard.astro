---
const { post } = Astro.props;
const formatDate = post.data.pubDate.toLocaleString('en-US', {
    //weekday: 'narrow', // long, short, narrow
    day: 'numeric', // numeric, 2-digit
    year: 'numeric', // numeric, 2-digit
    month: 'long', // numeric, 2-digit, long, short, narrow
    //hour: 'numeric', // numeric, 2-digit
    //minute: 'numeric', // numeric, 2-digit
    //second: 'numeric', // numeric, 2-digit
});
---

<a
  class="group w-full flex cursor-pointer flex-col gap-2 rounded-md border border-secondary-text p-4 transition-all duration-300 hover:-translate-y-2 hover:border-prime"
  href={`/blog/${post.slug}`}
>
  <div
    class="flex w-full flex-col justify-between gap-2 md:flex-row md:items-center"
  >
    <p class="group-hover:text-prime text-neutral-100">{post.data.title}</p>
    <div class="flex flex-row items-center gap-4">
      <p>{formatDate}</p>

      <svg
        width="18"
        height="18"
        viewBox="0 0 18 18"
        fill="none"
        class="transition-all duration-300 group-hover:translate-x-1"
      >
        <path
          d="M5.25 12.75L12.75 5.25"
          stroke="#999999"
          stroke-linecap="round"
          stroke-linejoin="round"></path>
        <path
          d="M5.25 5.25H12.75V12.75"
          stroke="#999999"
          stroke-linecap="round"
          stroke-linejoin="round"></path>
      </svg>
    </div>
  </div>

  <p class="truncate">{post.data.description}</p>
</a>
